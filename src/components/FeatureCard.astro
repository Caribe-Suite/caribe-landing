---
import { Check } from '@lucide/astro';

interface Props {
    title: string;
    description: string;
    icon: any; // Using any for the Lucide component
    items?: string[];
}

const { title, description, icon: Icon, items } = Astro.props;
---

<div class="bg-white p-6 sm:p-8 rounded-2xl shadow-sm border border-slate-100 hover:shadow-md transition-all duration-300 flex flex-col h-full">
    <div class="text-slate-800 mb-4">
        <Icon size={40} strokeWidth={1.5} aria-hidden="true" />
    </div>
    <h3 class="text-xl font-bold text-slate-800 mb-3">{title}</h3>
    <p class="text-slate-600 mb-4 leading-relaxed text-sm sm:text-base">
        {description}
    </p>
    {items && (
        <ul class="space-y-2 mt-auto">
            {items.map(item => (
                <li class="flex items-start text-sm text-slate-500">
                    <Check size={16} class="mr-2 mt-0.5 text-slate-400 shrink-0" aria-hidden="true" />
                    <span>{item}</span>
                </li>
            ))}
        </ul>
    )}
</div>
